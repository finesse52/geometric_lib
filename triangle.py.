def area(a, h):
    '''
    Возвращает площадь треугальника (a, b, c) в виде числа:
        Параметры:
                a (int) : Сторона треугольника
                h (int) : Высота треугольника с стороне a (a (int))
        Возвращаемое значение:
                a * h / 2
    '''
    return a * h / 2

def perimeter(a, b, c):
    '''
    Возвращает площадь треугальника (a, b, c) в виде числа:
        Параметры:
                a (int) : Первая сторона треугольника
                b (int) : Вторая сторона треугольника
                c (int) : Третья сторона треугольника
        Возвращаемое значение:
                a + b + c
    '''
    return a + b + c
